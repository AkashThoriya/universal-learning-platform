whatever we've implemented so far, verify every single file, every single line. check is this as per our expectation? are we updating everything in database using firebase with best practices? is this as per best practices? is this best UI/UX?  if not then please fix it. let's have perfect version. not just for this but for every feature in entire project.